# paper/ — P27 companions

LaTeX or markdown companions for **Paper 27 (SRRG)** may live here, mirroring
`ugp-physics-lean/paper/`.

Build artifacts (`*.aux`, `*.log`, …) are gitignored per `.gitignore`.

Prose license: when a draft is added, include `paper/LICENSE` if it is CC BY-NC-ND
(follow `ugp-physics/papers/LICENSE` pattern).
